[project]
name = "shared-ai"
version = "0.1.0"
description = "La Web Core - Shared AI clients (DeepSeek, embeddings)"
requires-python = ">=3.12"
dependencies = [
    "langchain>=0.3.0",
    "langchain-openai>=0.2.0",
    "langchain-community>=0.3.0",
    "fastembed>=0.2.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"
